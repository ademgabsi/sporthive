<form method="post" action="{{ path('app_reclamation_delete', {'ID_reclamation': reclamation.ID_reclamation}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reclamation.ID_reclamation) }}">
    <button class="btn">Delete</button>
</form>
