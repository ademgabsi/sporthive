{{ form_start(form) }}

<div class="form-group">
    {{ form_widget(form.nom, { attr: {'placeholder': ' '} }) }}
    <label for="{{ form.nom.vars.id }}">Nom du tournoi</label>
    {{ form_errors(form.nom) }}
</div>

<div class="dates-inline">
    <div class="form-group">
        {{ form_widget(form.date_debut, { attr: {'placeholder': ' '} }) }}
        <label for="{{ form.date_debut.vars.id }}">Date de début</label>
        {{ form_errors(form.date_debut) }}
    </div>
    <div class="form-group">
        {{ form_widget(form.date_fin, { attr: {'placeholder': ' '} }) }}
        <label for="{{ form.date_fin.vars.id }}">Date de fin</label>
        {{ form_errors(form.date_fin) }}
    </div>
</div>

<div class="form-group">
    {{ form_widget(form.lieu, { attr: {'placeholder': ' '} }) }}
    <label for="{{ form.lieu.vars.id }}">Lieu</label>
    {{ form_errors(form.lieu) }}
</div>

<div class="form-group">
    {{ form_widget(form.competition, { attr: {'placeholder': ' '} }) }}
    <label for="{{ form.competition.vars.id }}">Compétition</label>
    {{ form_errors(form.competition) }}
</div>

<button class="btn">{{ button_label|default('Créer') }}</button>

{{ form_end(form) }}
